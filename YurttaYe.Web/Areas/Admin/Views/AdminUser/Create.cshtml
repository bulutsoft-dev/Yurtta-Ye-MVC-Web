@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@model YurttaYe.Core.Models.ViewModels.CreateUserViewModel

@{
    ViewData["Title"] = "Yeni Kullanıcı Ekle";
    ViewData["BreadcrumbItems"] = new List<dynamic>
    {
        new { Text = "Kullanıcı Yönetimi", Icon = "fas fa-users", Url = Url.Action("Index", "AdminUser", new { area = "Admin" }) },
        new { Text = "Yeni Kullanıcı", Icon = "fas fa-user-plus", Url = (string?)null }
    };
}

@Html.AntiForgeryToken()

<!-- Main Content -->
<div class="max-w-2xl mx-auto">
    <!-- Info Card -->
    <div class="admin-card bg-gradient-to-r from-blue-50 to-indigo-50 rounded-xl p-6 mb-6 border border-blue-200">
        <div class="flex items-center">
            <div class="w-12 h-12 bg-gradient-to-r from-blue-500 to-indigo-600 rounded-xl flex items-center justify-center mr-4">
                <i class="fas fa-user-plus text-white text-xl"></i>
            </div>
            <div>
                <h3 class="text-lg font-semibold text-gray-900">Yeni Kullanıcı Oluştur</h3>
                <p class="text-sm text-gray-600">Admin panelini kullanacak yeni bir kullanıcı ekleyin.</p>
            </div>
        </div>
    </div>

    <!-- Form Card -->
    <div class="admin-card bg-white rounded-xl shadow-lg border border-gray-200 overflow-hidden">
        <div class="bg-gradient-to-r from-blue-600 to-indigo-600 px-6 py-4">
            <h2 class="text-xl font-bold text-white flex items-center">
                <i class="fas fa-user mr-3"></i>
                Kullanıcı Bilgileri
            </h2>
        </div>

        <form asp-area="Admin" asp-controller="AdminUser" asp-action="Create" method="post" class="p-6">
            @Html.AntiForgeryToken()
            
            <!-- Validation Summary -->
            @if (!ViewData.ModelState.IsValid)
            {
                <div class="mb-6 bg-red-50 border border-red-200 rounded-lg p-4">
                    <div class="flex items-start">
                        <i class="fas fa-exclamation-circle text-red-500 mr-3 mt-0.5"></i>
                        <div>
                            <h4 class="text-red-800 font-medium mb-2">Lütfen hataları düzeltin:</h4>
                            <div asp-validation-summary="All" class="text-red-700 text-sm"></div>
                        </div>
                    </div>
                </div>
            }
            
            <!-- Username -->
            <div class="mb-5">
                <label asp-for="UserName" class="block text-sm font-semibold text-gray-700 mb-2">
                    <i class="fas fa-user text-blue-500 mr-2"></i>
                    Kullanıcı Adı <span class="text-red-500">*</span>
                </label>
                <div class="relative">
                    <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                        <i class="fas fa-at text-gray-400"></i>
                    </div>
                    <input asp-for="UserName" 
                           class="w-full pl-10 pr-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors text-gray-900" 
                           placeholder="örnek: admin_user" />
                </div>
                <span asp-validation-for="UserName" class="text-red-500 text-sm mt-1"></span>
            </div>

            <!-- Email -->
            <div class="mb-5">
                <label asp-for="Email" class="block text-sm font-semibold text-gray-700 mb-2">
                    <i class="fas fa-envelope text-blue-500 mr-2"></i>
                    E-posta <span class="text-red-500">*</span>
                </label>
                <div class="relative">
                    <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                        <i class="fas fa-envelope text-gray-400"></i>
                    </div>
                    <input asp-for="Email" type="email"
                           class="w-full pl-10 pr-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors text-gray-900" 
                           placeholder="ornek@example.com" />
                </div>
                <span asp-validation-for="Email" class="text-red-500 text-sm mt-1"></span>
            </div>

            <!-- Phone -->
            <div class="mb-5">
                <label asp-for="PhoneNumber" class="block text-sm font-semibold text-gray-700 mb-2">
                    <i class="fas fa-phone text-blue-500 mr-2"></i>
                    Telefon
                </label>
                <div class="relative">
                    <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                        <i class="fas fa-phone text-gray-400"></i>
                    </div>
                    <input asp-for="PhoneNumber" type="tel"
                           class="w-full pl-10 pr-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors text-gray-900" 
                           placeholder="+90 5XX XXX XX XX" />
                </div>
                <span asp-validation-for="PhoneNumber" class="text-red-500 text-sm mt-1"></span>
            </div>

            <!-- Password Section -->
            <div class="bg-gray-50 rounded-lg p-4 mb-5">
                <h4 class="text-sm font-semibold text-gray-700 mb-4 flex items-center">
                    <i class="fas fa-lock text-blue-500 mr-2"></i>
                    Şifre Bilgileri
                </h4>
                
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <!-- Password -->
                    <div>
                        <label asp-for="Password" class="block text-sm font-medium text-gray-700 mb-2">
                            Şifre <span class="text-red-500">*</span>
                        </label>
                        <div class="relative">
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                <i class="fas fa-key text-gray-400"></i>
                            </div>
                            <input asp-for="Password" type="password" id="password"
                                   class="w-full pl-10 pr-10 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors text-gray-900" 
                                   placeholder="••••••••" />
                            <button type="button" onclick="togglePassword('password')" class="absolute inset-y-0 right-0 pr-3 flex items-center">
                                <i class="fas fa-eye text-gray-400 hover:text-gray-600" id="password-toggle"></i>
                            </button>
                        </div>
                        <span asp-validation-for="Password" class="text-red-500 text-sm mt-1"></span>
                    </div>

                    <!-- Confirm Password -->
                    <div>
                        <label asp-for="ConfirmPassword" class="block text-sm font-medium text-gray-700 mb-2">
                            Şifre Tekrar <span class="text-red-500">*</span>
                        </label>
                        <div class="relative">
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                <i class="fas fa-key text-gray-400"></i>
                            </div>
                            <input asp-for="ConfirmPassword" type="password" id="confirm-password"
                                   class="w-full pl-10 pr-10 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors text-gray-900" 
                                   placeholder="••••••••" />
                            <button type="button" onclick="togglePassword('confirm-password')" class="absolute inset-y-0 right-0 pr-3 flex items-center">
                                <i class="fas fa-eye text-gray-400 hover:text-gray-600" id="confirm-password-toggle"></i>
                            </button>
                        </div>
                        <span asp-validation-for="ConfirmPassword" class="text-red-500 text-sm mt-1"></span>
                    </div>
                </div>
                
                <p class="text-xs text-gray-500 mt-3">
                    <i class="fas fa-info-circle mr-1"></i>
                    Şifre en az 6 karakter olmalı ve büyük harf, küçük harf, rakam içermelidir.
                </p>
            </div>

            <!-- Options -->
            <div class="bg-blue-50 rounded-lg p-4 mb-6">
                <h4 class="text-sm font-semibold text-gray-700 mb-4 flex items-center">
                    <i class="fas fa-cog text-blue-500 mr-2"></i>
                    Kullanıcı Ayarları
                </h4>
                
                <div class="space-y-3">
                    <label class="flex items-center cursor-pointer">
                        <input asp-for="IsAdmin" type="checkbox" 
                               class="w-5 h-5 text-blue-600 border-gray-300 rounded focus:ring-blue-500 mr-3" />
                        <div>
                            <span class="font-medium text-gray-900">Admin Rolü</span>
                            <p class="text-xs text-gray-500">Kullanıcıya admin paneli erişim yetkisi ver</p>
                        </div>
                    </label>
                    
                    <label class="flex items-center cursor-pointer">
                        <input asp-for="EmailConfirmed" type="checkbox" 
                               class="w-5 h-5 text-green-600 border-gray-300 rounded focus:ring-green-500 mr-3" />
                        <div>
                            <span class="font-medium text-gray-900">E-posta Onaylı</span>
                            <p class="text-xs text-gray-500">E-posta doğrulamasını atla</p>
                        </div>
                    </label>
                </div>
            </div>

            <!-- Divider -->
            <div class="border-t border-gray-200 my-6"></div>

            <!-- Action Buttons -->
            <div class="flex items-center justify-between">
                <a asp-area="Admin" asp-controller="AdminUser" asp-action="Index" 
                   class="inline-flex items-center px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition-colors">
                    <i class="fas fa-arrow-left mr-2"></i>
                    Listeye Dön
                </a>
                
                <div class="flex items-center space-x-3">
                    <button type="reset" 
                            class="inline-flex items-center px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition-colors">
                        <i class="fas fa-undo mr-2"></i>
                        Temizle
                    </button>
                    <button type="submit" 
                            class="inline-flex items-center px-6 py-2 bg-gradient-to-r from-blue-600 to-indigo-600 text-white rounded-lg hover:from-blue-700 hover:to-indigo-700 transition-all shadow-lg transform hover:scale-105">
                        <i class="fas fa-user-plus mr-2"></i>
                        Kullanıcı Oluştur
                    </button>
                </div>
            </div>
        </form>
    </div>
</div>

@if (TempData["Success"] != null)
{
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            showAdminNotification('@TempData["Success"]', 'success');
        });
    </script>
}
@if (TempData["Error"] != null)
{
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            showAdminNotification('@TempData["Error"]', 'error');
        });
    </script>
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function togglePassword(inputId) {
            const input = document.getElementById(inputId);
            const icon = document.getElementById(inputId + '-toggle');
            
            if (input.type === 'password') {
                input.type = 'text';
                icon.classList.remove('fa-eye');
                icon.classList.add('fa-eye-slash');
            } else {
                input.type = 'password';
                icon.classList.remove('fa-eye-slash');
                icon.classList.add('fa-eye');
            }
        }
    </script>
}
