@model YurttaYe.Core.Models.ViewModels.DashboardViewModel
@{
    ViewData["Title"] = "Dashboard";
    ViewData["Subtitle"] = "Yönetim Paneli";
}

<!-- Enhanced Stats Cards -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
    <!-- Total Menus Card -->
    <div class="admin-card bg-gradient-to-br from-blue-500 via-blue-600 to-blue-700 rounded-xl p-6 text-white shadow-xl transform hover:scale-105 transition-all duration-300">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-blue-100 text-sm font-medium uppercase tracking-wide">Toplam Menü</p>
                <p class="text-4xl font-bold mb-2 animate-pulse">@Model.TotalMenus</p>
                <div class="flex items-center">
                    <i class="fas fa-trending-up text-green-300 mr-2"></i>
                    <span class="text-sm text-blue-100">Bu ay +@((DateTime.Now.Day * 2)) yeni</span>
                </div>
            </div>
            <div class="bg-white bg-opacity-20 rounded-full p-4 backdrop-blur-sm">
                <i class="fas fa-utensils text-3xl"></i>
            </div>
        </div>
        <div class="mt-6 pt-4 border-t border-blue-400 border-opacity-30">
            <a asp-area="Admin" asp-controller="AdminMenu" asp-action="Index" 
               class="flex items-center justify-between text-blue-100 hover:text-white transition-colors group">
                <span class="font-medium">Menüleri Yönet</span>
                <i class="fas fa-arrow-right group-hover:translate-x-1 transition-transform"></i>
            </a>
        </div>
    </div>

    <!-- Total Cities Card -->
    <div class="admin-card bg-gradient-to-br from-emerald-500 via-emerald-600 to-emerald-700 rounded-xl p-6 text-white shadow-xl transform hover:scale-105 transition-all duration-300">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-emerald-100 text-sm font-medium uppercase tracking-wide">Kayıtlı Şehir</p>
                <p class="text-4xl font-bold mb-2">@Model.TotalCities</p>
                <div class="flex items-center">
                    <i class="fas fa-map-marked-alt text-emerald-300 mr-2"></i>
                    <span class="text-sm text-emerald-100">Aktif lokasyon</span>
                </div>
            </div>
            <div class="bg-white bg-opacity-20 rounded-full p-4 backdrop-blur-sm">
                <i class="fas fa-city text-3xl"></i>
            </div>
        </div>
        <div class="mt-6 pt-4 border-t border-emerald-400 border-opacity-30">
            <a asp-area="Admin" asp-controller="AdminCity" asp-action="Index" 
               class="flex items-center justify-between text-emerald-100 hover:text-white transition-colors group">
                <span class="font-medium">Şehirleri Yönet</span>
                <i class="fas fa-arrow-right group-hover:translate-x-1 transition-transform"></i>
            </a>
        </div>
    </div>

    <!-- Today's Menus Card -->
    <div class="admin-card bg-gradient-to-br from-purple-500 via-purple-600 to-purple-700 rounded-xl p-6 text-white shadow-xl transform hover:scale-105 transition-all duration-300">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-purple-100 text-sm font-medium uppercase tracking-wide">Bugünkü Menüler</p>
                <p class="text-4xl font-bold mb-2" id="today-menus">@(Model.RecentMenus?.Where(m => m.Date.Date == DateTime.Today).Count() ?? 0)</p>
                <div class="flex items-center">
                    <i class="fas fa-calendar-day text-purple-300 mr-2"></i>
                    <span class="text-sm text-purple-100">@DateTime.Today.ToString("dd MMMM")</span>
                </div>
            </div>
            <div class="bg-white bg-opacity-20 rounded-full p-4 backdrop-blur-sm">
                <i class="fas fa-clock text-3xl"></i>
            </div>
        </div>
        <div class="mt-6 pt-4 border-t border-purple-400 border-opacity-30">
            <button onclick="refreshTodayMenus()" 
                    class="flex items-center justify-between text-purple-100 hover:text-white transition-colors group w-full">
                <span class="font-medium">Bugünü Yenile</span>
                <i class="fas fa-sync-alt group-hover:rotate-180 transition-transform duration-500"></i>
            </button>
        </div>
    </div>

    <!-- System Health Card -->
    <div class="admin-card bg-gradient-to-br from-orange-500 via-orange-600 to-orange-700 rounded-xl p-6 text-white shadow-xl transform hover:scale-105 transition-all duration-300">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-orange-100 text-sm font-medium uppercase tracking-wide">Sistem Sağlığı</p>
                <p class="text-4xl font-bold mb-2">
                    <span id="system-health" class="animate-pulse">98</span><span class="text-2xl">%</span>
                </p>
                <div class="flex items-center">
                    <div class="w-2 h-2 bg-green-400 rounded-full mr-2 animate-pulse"></div>
                    <span class="text-sm text-orange-100">Çevrimiçi</span>
                </div>
            </div>
            <div class="bg-white bg-opacity-20 rounded-full p-4 backdrop-blur-sm">
                <i class="fas fa-heartbeat text-3xl"></i>
            </div>
        </div>
        <div class="mt-6 pt-4 border-t border-orange-400 border-opacity-30">
            <button onclick="checkSystemHealth()" 
                    class="flex items-center justify-between text-orange-100 hover:text-white transition-colors group w-full">
                <span class="font-medium">Durumu Kontrol Et</span>
                <i class="fas fa-stethoscope group-hover:scale-110 transition-transform"></i>
            </button>
        </div>
    </div>
</div>

<!-- Enhanced Quick Actions & Monthly Distribution -->
<div class="grid grid-cols-1 xl:grid-cols-4 gap-6 mb-8">
    <!-- Enhanced Quick Actions -->
    <div class="xl:col-span-1 admin-card bg-white rounded-xl shadow-lg border border-gray-100 p-6">
        <h3 class="text-xl font-bold text-gray-900 mb-6 flex items-center">
            <div class="w-8 h-8 bg-gradient-to-r from-blue-500 to-purple-600 rounded-lg flex items-center justify-center mr-3">
                <i class="fas fa-bolt text-white text-sm"></i>
            </div>
            Hızlı İşlemler
        </h3>
        
        <div class="space-y-4">
            <!-- Add Menu -->
            <a asp-area="Admin" asp-controller="AdminMenu" asp-action="Create" 
               class="group flex items-center p-4 rounded-xl border-2 border-blue-100 hover:border-blue-300 hover:bg-blue-50 transition-all duration-300 nav-link">
                <div class="w-12 h-12 bg-blue-100 group-hover:bg-blue-200 rounded-xl flex items-center justify-center mr-4 transition-colors">
                    <i class="fas fa-plus text-blue-600 text-lg group-hover:scale-110 transition-transform"></i>
                </div>
                <div>
                    <p class="font-semibold text-gray-900 group-hover:text-blue-700">Yeni Menü</p>
                    <p class="text-sm text-gray-500">Menü ekle</p>
                </div>
            </a>

            <!-- Add City -->
            <a asp-area="Admin" asp-controller="AdminCity" asp-action="Create" 
               class="group flex items-center p-4 rounded-xl border-2 border-emerald-100 hover:border-emerald-300 hover:bg-emerald-50 transition-all duration-300 nav-link">
                <div class="w-12 h-12 bg-emerald-100 group-hover:bg-emerald-200 rounded-xl flex items-center justify-center mr-4 transition-colors">
                    <i class="fas fa-map-marker-alt text-emerald-600 text-lg group-hover:scale-110 transition-transform"></i>
                </div>
                <div>
                    <p class="font-semibold text-gray-900 group-hover:text-emerald-700">Yeni Şehir</p>
                    <p class="text-sm text-gray-500">Şehir ekle</p>
                </div>
            </a>

            <!-- Bulk Import -->
            <button onclick="showBulkImportModal()" 
                    class="group flex items-center p-4 rounded-xl border-2 border-purple-100 hover:border-purple-300 hover:bg-purple-50 transition-all duration-300 w-full">
                <div class="w-12 h-12 bg-purple-100 group-hover:bg-purple-200 rounded-xl flex items-center justify-center mr-4 transition-colors">
                    <i class="fas fa-file-import text-purple-600 text-lg group-hover:scale-110 transition-transform"></i>
                </div>
                <div>
                    <p class="font-semibold text-gray-900 group-hover:text-purple-700">Toplu İçe Aktarım</p>
                    <p class="text-sm text-gray-500">Excel'den yükle</p>
                </div>
            </button>

            <!-- Export Data -->
            <button onclick="exportAllData()" 
                    class="group flex items-center p-4 rounded-xl border-2 border-orange-100 hover:border-orange-300 hover:bg-orange-50 transition-all duration-300 w-full">
                <div class="w-12 h-12 bg-orange-100 group-hover:bg-orange-200 rounded-xl flex items-center justify-center mr-4 transition-colors">
                    <i class="fas fa-download text-orange-600 text-lg group-hover:scale-110 transition-transform"></i>
                </div>
                <div>
                    <p class="font-semibold text-gray-900 group-hover:text-orange-700">Veri Dışa Aktar</p>
                    <p class="text-sm text-gray-500">Excel raporu</p>
                </div>
            </button>

            <!-- Settings -->
            <a asp-area="Admin" asp-controller="Settings" asp-action="Index" 
               class="group flex items-center p-4 rounded-xl border-2 border-gray-100 hover:border-gray-300 hover:bg-gray-50 transition-all duration-300 nav-link">
                <div class="w-12 h-12 bg-gray-100 group-hover:bg-gray-200 rounded-xl flex items-center justify-center mr-4 transition-colors">
                    <i class="fas fa-cog text-gray-600 text-lg group-hover:scale-110 transition-transform"></i>
                </div>
                <div>
                    <p class="font-semibold text-gray-900 group-hover:text-gray-700">Ayarlar</p>
                    <p class="text-sm text-gray-500">Sistem ayarları</p>
                </div>
            </a>
        </div>
    </div>

    <!-- Enhanced Monthly Menu Distribution -->
    <div class="xl:col-span-3 admin-card bg-white rounded-xl shadow-lg border border-gray-100 p-6">
        <div class="flex items-center justify-between mb-6">
            <h3 class="text-xl font-bold text-gray-900 flex items-center">
                <div class="w-8 h-8 bg-gradient-to-r from-blue-500 to-purple-600 rounded-lg flex items-center justify-center mr-3">
                    <i class="fas fa-chart-line text-white text-sm"></i>
                </div>
                Aylık Menü Dağılımı
            </h3>
            <div class="flex items-center space-x-2">
                <button onclick="updateChartPeriod('6months')" 
                        class="chart-period-btn active px-4 py-2 text-sm font-medium rounded-lg bg-blue-100 text-blue-700 hover:bg-blue-200 transition-colors">
                    6 Ay
                </button>
                <button onclick="updateChartPeriod('1year')" 
                        class="chart-period-btn px-4 py-2 text-sm font-medium rounded-lg bg-gray-100 text-gray-600 hover:bg-gray-200 transition-colors">
                    1 Yıl
                </button>
                <button onclick="refreshChart()" 
                        class="p-2 text-gray-400 hover:text-gray-600 rounded-lg hover:bg-gray-100 transition-colors">
                    <i class="fas fa-sync-alt"></i>
                </button>
            </div>
        </div>
        
        <!-- Chart Container with Loading -->
        <div class="relative h-80 bg-gray-50 rounded-lg p-4">
            <canvas id="menuDistributionChart" class="w-full h-full"></canvas>
            <div id="chart-loading" class="absolute inset-0 flex items-center justify-center bg-white bg-opacity-90 rounded-lg hidden">
                <div class="text-center">
                    <i class="fas fa-spinner animate-spin text-3xl text-blue-500 mb-3"></i>
                    <p class="text-gray-600 font-medium">Grafik yükleniyor...</p>
                </div>
            </div>
        </div>

        <!-- Chart Legend & Stats -->
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mt-6 pt-6 border-t border-gray-100">
            <div class="text-center">
                <div class="w-4 h-4 bg-blue-500 rounded-full mx-auto mb-2"></div>
                <p class="text-sm font-medium text-gray-600">Sabah Menüleri</p>
                <p class="text-lg font-bold text-gray-900" id="morning-count">@(Model.RecentMenus?.Where(m => m.MealType == "Sabah").Count() ?? 0)</p>
            </div>
            <div class="text-center">
                <div class="w-4 h-4 bg-purple-500 rounded-full mx-auto mb-2"></div>
                <p class="text-sm font-medium text-gray-600">Akşam Menüleri</p>
                <p class="text-lg font-bold text-gray-900" id="evening-count">@(Model.RecentMenus?.Where(m => m.MealType == "Akşam").Count() ?? 0)</p>
            </div>
            <div class="text-center">
                <div class="w-4 h-4 bg-emerald-500 rounded-full mx-auto mb-2"></div>
                <p class="text-sm font-medium text-gray-600">Bu Ay</p>
                <p class="text-lg font-bold text-gray-900" id="this-month-count">@(Model.RecentMenus?.Where(m => m.Date.Month == DateTime.Now.Month).Count() ?? 0)</p>
            </div>
            <div class="text-center">
                <div class="w-4 h-4 bg-orange-500 rounded-full mx-auto mb-2"></div>
                <p class="text-sm font-medium text-gray-600">Toplam</p>
                <p class="text-lg font-bold text-gray-900">@Model.TotalMenus</p>
            </div>
        </div>
    </div>
</div>

<!-- Enhanced Recent Menus Table -->
<div class="admin-card bg-white rounded-xl shadow-lg border border-gray-100">
    <div class="p-6 border-b border-gray-100">
        <div class="flex items-center justify-between">
            <h3 class="text-xl font-bold text-gray-900 flex items-center">
                <div class="w-8 h-8 bg-gradient-to-r from-green-500 to-blue-600 rounded-lg flex items-center justify-center mr-3">
                    <i class="fas fa-history text-white text-sm"></i>
                </div>
                Son Eklenen Menüler
            </h3>
            <div class="flex items-center space-x-3">
                <div class="flex items-center space-x-2">
                    <label class="text-sm text-gray-600">Filtre:</label>
                    <select id="menu-filter" onchange="filterMenus(this.value)" 
                            class="px-3 py-1 text-sm border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                        <option value="all">Tümü</option>
                        <option value="Sabah">Sabah</option>
                        <option value="Akşam">Akşam</option>
                        <option value="today">Bugün</option>
                    </select>
                </div>
                <button onclick="refreshMenuTable()" 
                        class="p-2 text-gray-400 hover:text-gray-600 rounded-lg hover:bg-gray-100 transition-colors">
                    <i class="fas fa-sync-alt"></i>
                </button>
                <a asp-area="Admin" asp-controller="AdminMenu" asp-action="Index" 
                   class="inline-flex items-center px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700 transition-colors">
                    <i class="fas fa-list mr-2"></i>
                    Tümünü Gör
                </a>
            </div>
        </div>
    </div>
    
    <div class="overflow-x-auto" id="recent-menus-table">
        @if (Model.RecentMenus != null && Model.RecentMenus.Any())
        {
            <table class="w-full">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">Şehir & Lokasyon</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">Öğün Türü</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">Tarih</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">Menü Özeti</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">Durum</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">İşlemler</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-100">
                    @foreach (var menu in Model.RecentMenus.Take(8))
                    {
                        <tr class="hover:bg-gray-50 transition-colors duration-200 menu-row" data-meal-type="@menu.MealType" data-date="@menu.Date.ToString("yyyy-MM-dd")">
                            <td class="px-6 py-4 whitespace-nowrap">
                                <div class="flex items-center">
                                    <div class="w-10 h-10 bg-gradient-to-r from-blue-500 to-purple-600 rounded-lg flex items-center justify-center mr-3">
                                        <i class="fas fa-map-marker-alt text-white text-sm"></i>
                                    </div>
                                    <div>
                                        <p class="text-sm font-semibold text-gray-900">@menu.CityName</p>
                                        <p class="text-xs text-gray-500">@menu.Location</p>
                                    </div>
                                </div>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <span class="inline-flex items-center px-3 py-1 text-xs font-semibold rounded-full 
                                      @(menu.MealType == "Sabah" ? "bg-yellow-100 text-yellow-800 border border-yellow-200" : 
                                        menu.MealType == "Akşam" ? "bg-blue-100 text-blue-800 border border-blue-200" : "bg-gray-100 text-gray-800 border border-gray-200")">
                                    <i class="fas @(menu.MealType == "Sabah" ? "fa-sun" : "fa-moon") mr-1"></i>
                                    @menu.MealType
                                </span>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <div class="text-sm text-gray-900 font-medium">@menu.Date.ToString("dd.MM.yyyy")</div>
                                <div class="text-xs text-gray-500">@menu.Date.ToString("dddd")</div>
                            </td>
                            <td class="px-6 py-4">
                                <div class="text-sm text-gray-900 max-w-xs truncate" title="@menu.MenuContent">
                                    @(menu.MenuContent?.Length > 50 ? menu.MenuContent.Substring(0, 50) + "..." : menu.MenuContent)
                                </div>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <span class="inline-flex items-center px-2 py-1 text-xs font-semibold rounded-full bg-green-100 text-green-800 border border-green-200">
                                    <div class="w-2 h-2 bg-green-500 rounded-full mr-2"></div>
                                    Aktif
                                </span>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                                <div class="flex items-center space-x-2">
                                    <a asp-area="Admin" asp-controller="AdminMenu" asp-action="Edit" asp-route-id="@menu.Id" 
                                       class="nav-link inline-flex items-center px-3 py-2 text-xs font-medium rounded-lg text-blue-600 bg-blue-50 hover:bg-blue-100 transition-colors">
                                        <i class="fas fa-edit mr-1"></i>
                                        Düzenle
                                    </a>
                                    <a asp-area="Admin" asp-controller="AdminMenu" asp-action="Details" asp-route-id="@menu.Id" 
                                       class="nav-link inline-flex items-center px-3 py-2 text-xs font-medium rounded-lg text-gray-600 bg-gray-50 hover:bg-gray-100 transition-colors">
                                        <i class="fas fa-eye mr-1"></i>
                                        Görüntüle
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            
            @if (Model.RecentMenus.Count() > 8)
            {
                <div class="px-6 py-4 bg-gray-50 border-t border-gray-100 text-center">
                    <p class="text-sm text-gray-600 mb-3">@(Model.RecentMenus.Count() - 8) menü daha var</p>
                    <a asp-area="Admin" asp-controller="AdminMenu" asp-action="Index" 
                       class="inline-flex items-center px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700 transition-colors">
                        <i class="fas fa-list mr-2"></i>
                        Tüm Menüleri Görüntüle
                    </a>
                </div>
            }
        }
        else
        {
            <div class="p-12 text-center">
                <div class="w-20 h-20 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-4">
                    <i class="fas fa-utensils text-3xl text-gray-400"></i>
                </div>
                <h3 class="text-lg font-semibold text-gray-900 mb-2">Henüz menü eklenmemiş</h3>
                <p class="text-gray-500 mb-6">Sistem yönetimi için ilk menüyü ekleyerek başlayın</p>
                <a asp-area="Admin" asp-controller="AdminMenu" asp-action="Create" 
                   class="inline-flex items-center px-6 py-3 bg-blue-600 text-white font-medium rounded-lg hover:bg-blue-700 transition-colors shadow-lg">
                    <i class="fas fa-plus mr-2"></i>
                    İlk Menüyü Ekle
                </a>
            </div>
        }
    </div>
</div>

<!-- Bulk Import Modal -->
<div id="bulk-import-modal" class="fixed inset-0 bg-black bg-opacity-50 hidden z-50 flex items-center justify-center">
    <div class="bg-white rounded-xl shadow-2xl max-w-md w-full mx-4 transform transition-all duration-300">
        <div class="p-6 border-b border-gray-200">
            <div class="flex items-center justify-between">
                <h3 class="text-lg font-bold text-gray-900">Toplu İçe Aktarım</h3>
                <button onclick="closeBulkImportModal()" class="text-gray-400 hover:text-gray-600">
                    <i class="fas fa-times text-xl"></i>
                </button>
            </div>
        </div>
        <div class="p-6">
            <div class="text-center mb-6">
                <div class="w-16 h-16 bg-purple-100 rounded-full flex items-center justify-center mx-auto mb-4">
                    <i class="fas fa-file-excel text-2xl text-purple-600"></i>
                </div>
                <p class="text-gray-600">Excel dosyasından menüleri toplu olarak yükleyebilirsiniz</p>
            </div>
            
            <div class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Excel Dosyası Seçin</label>
                    <input type="file" id="bulk-import-file" accept=".xlsx,.xls" 
                           class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500">
                </div>
                
                <div class="flex items-center space-x-3">
                    <button onclick="downloadTemplate()" 
                            class="flex-1 px-4 py-2 text-sm bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition-colors">
                        <i class="fas fa-download mr-2"></i>
                        Şablon İndir
                    </button>
                    <button onclick="processBulkImport()" 
                            class="flex-1 px-4 py-2 text-sm bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors">
                        <i class="fas fa-upload mr-2"></i>
                        Yükle
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        let menuDistributionChart;
        let currentPeriod = '6months';
        
        // Initialize Dashboard
        document.addEventListener('DOMContentLoaded', function() {
            initializeChart();
            setupRealTimeUpdates();
            animateCounters();
        });
        
        // Initialize Enhanced Chart
        function initializeChart() {
            const ctx = document.getElementById('menuDistributionChart').getContext('2d');
            
            // Sample data - replace with actual data from backend
            const monthlyData = {
                '6months': {
                    labels: ['Şub', 'Mar', 'Nis', 'May', 'Haz', 'Tem'],
                    morning: [12, 19, 15, 25, 22, 30],
                    evening: [8, 15, 12, 20, 18, 25]
                },
                '1year': {
                    labels: ['Oca', 'Şub', 'Mar', 'Nis', 'May', 'Haz', 'Tem', 'Ağu', 'Eyl', 'Eki', 'Kas', 'Ara'],
                    morning: [15, 12, 19, 15, 25, 22, 30, 28, 24, 20, 18, 22],
                    evening: [10, 8, 15, 12, 20, 18, 25, 23, 19, 16, 14, 18]
                }
            };
            
            menuDistributionChart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: monthlyData[currentPeriod].labels,
                    datasets: [
                        {
                            label: 'Sabah Menüleri',
                            data: monthlyData[currentPeriod].morning,
                            borderColor: '#3B82F6',
                            backgroundColor: 'rgba(59, 130, 246, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#3B82F6',
                            pointBorderColor: '#ffffff',
                            pointBorderWidth: 2,
                            pointRadius: 6
                        },
                        {
                            label: 'Akşam Menüleri',
                            data: monthlyData[currentPeriod].evening,
                            borderColor: '#8B5CF6',
                            backgroundColor: 'rgba(139, 92, 246, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true,
                            pointBackgroundColor: '#8B5CF6',
                            pointBorderColor: '#ffffff',
                            pointBorderWidth: 2,
                            pointRadius: 6
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: {
                        intersect: false,
                        mode: 'index'
                    },
                    plugins: {
                        legend: {
                            display: false
                        },
                        tooltip: {
                            backgroundColor: 'rgba(17, 24, 39, 0.9)',
                            titleColor: '#F9FAFB',
                            bodyColor: '#F9FAFB',
                            borderColor: '#374151',
                            borderWidth: 1,
                            cornerRadius: 8
                        }
                    },
                    scales: {
                        y: {
                            beginAtZero: true,
                            grid: {
                                color: 'rgba(156, 163, 175, 0.2)',
                                drawBorder: false
                            },
                            ticks: {
                                color: '#6B7280',
                                font: {
                                    size: 12
                                }
                            }
                        },
                        x: {
                            grid: {
                                display: false
                            },
                            ticks: {
                                color: '#6B7280',
                                font: {
                                    size: 12,
                                    weight: '500'
                                }
                            }
                        }
                    }
                });
        }
        
        // Update Chart Period
        function updateChartPeriod(period) {
            currentPeriod = period;
            
            // Update button states
            document.querySelectorAll('.chart-period-btn').forEach(btn => {
                btn.classList.remove('active', 'bg-blue-100', 'text-blue-700');
                btn.classList.add('bg-gray-100', 'text-gray-600');
            });
            
            event.target.classList.remove('bg-gray-100', 'text-gray-600');
            event.target.classList.add('active', 'bg-blue-100', 'text-blue-700');
            
            refreshChart();
        }
        
        // Refresh Chart
        function refreshChart() {
            const loadingEl = document.getElementById('chart-loading');
            loadingEl.classList.remove('hidden');
            
            setTimeout(() => {
                // Simulate data loading
                initializeChart();
                loadingEl.classList.add('hidden');
            }, 1000);
        }
        
        // Dashboard Functions
        function refreshTodayMenus() {
            const el = document.getElementById('today-menus');
            el.style.opacity = '0.5';
            
            setTimeout(() => {
                // Simulate refresh - replace with actual API call
                const newCount = Math.floor(Math.random() * 10) + 1;
                el.textContent = newCount;
                el.style.opacity = '1';
                
                showNotification('Bugünkü menüler yenilendi!', 'success');
            }, 1000);
        }
        
        function checkSystemHealth() {
            const el = document.getElementById('system-health');
            el.style.opacity = '0.5';
            
            setTimeout(() => {
                const health = Math.floor(Math.random() * 5) + 95;
                el.textContent = health;
                el.style.opacity = '1';
                
                showNotification('Sistem durumu kontrol edildi!', 'info');
            }, 1500);
        }
        
        function refreshMenuTable() {
            const table = document.getElementById('recent-menus-table');
            table.style.opacity = '0.5';
            
            setTimeout(() => {
                table.style.opacity = '1';
                showNotification('Menü listesi yenilendi!', 'success');
            }, 800);
        }
        
        // Filter Functions
        function filterMenus(filter) {
            const rows = document.querySelectorAll('.menu-row');
            const today = new Date().toISOString().split('T')[0];
            
            rows.forEach(row => {
                const mealType = row.dataset.mealType;
                const date = row.dataset.date;
                
                let show = true;
                
                if (filter === 'Sabah' && mealType !== 'Sabah') show = false;
                if (filter === 'Akşam' && mealType !== 'Akşam') show = false;
                if (filter === 'today' && date !== today) show = false;
                
                row.style.display = show ? '' : 'none';
            });
        }
        
        // Bulk Import Functions
        function showBulkImportModal() {
            document.getElementById('bulk-import-modal').classList.remove('hidden');
        }
        
        function closeBulkImportModal() {
            document.getElementById('bulk-import-modal').classList.add('hidden');
        }
        
        function downloadTemplate() {
            // Simulate template download
            showNotification('Excel şablonu indiriliyor...', 'info');
        }
        
        function processBulkImport() {
            const fileInput = document.getElementById('bulk-import-file');
            if (!fileInput.files.length) {
                showNotification('Lütfen bir Excel dosyası seçin!', 'error');
                return;
            }
            
            showNotification('Dosya işleniyor...', 'info');
            closeBulkImportModal();
        }
        
        function exportAllData() {
            showNotification('Veriler Excel formatında dışa aktarılıyor...', 'info');
        }
        
        // Utility Functions
        function animateCounters() {
            // Animate counter numbers on page load
            const counters = document.querySelectorAll('[id$="-count"]');
            counters.forEach(counter => {
                const target = parseInt(counter.textContent);
                let current = 0;
                const increment = target / 20;
                
                const timer = setInterval(() => {
                    current += increment;
                    if (current >= target) {
                        counter.textContent = target;
                        clearInterval(timer);
                    } else {
                        counter.textContent = Math.floor(current);
                    }
                }, 50);
            });
        }
        
        function setupRealTimeUpdates() {
            // Setup periodic updates for dashboard data
            setInterval(() => {
                // Update system health randomly
                const healthEl = document.getElementById('system-health');
                if (healthEl) {
                    const newHealth = Math.floor(Math.random() * 5) + 95;
                    healthEl.textContent = newHealth;
                }
            }, 30000); // Update every 30 seconds
        }
        
        function showNotification(message, type = 'info') {
            const colors = {
                success: 'bg-green-500',
                error: 'bg-red-500',
                info: 'bg-blue-500',
                warning: 'bg-yellow-500'
            };
            
            const notification = document.createElement('div');
            notification.className = `fixed top-4 right-4 ${colors[type]} text-white px-6 py-3 rounded-lg shadow-lg z-50 transform translate-x-full transition-transform duration-300`;
            notification.innerHTML = `
                <div class="flex items-center">
                    <i class="fas fa-${type === 'success' ? 'check' : type === 'error' ? 'exclamation-triangle' : 'info-circle'} mr-2"></i>
                    <span>${message}</span>
                </div>
            `;
            
            document.body.appendChild(notification);
            
            setTimeout(() => {
                notification.classList.remove('translate-x-full');
            }, 100);
            
            setTimeout(() => {
                notification.classList.add('translate-x-full');
                setTimeout(() => notification.remove(), 300);
            }, 3000);
        }
    </script>
}

<style>
    .admin-card {
        transition: all 0.3s ease;
    }
    
    .admin-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    }
    
    .chart-period-btn.active {
        box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.2);
    }
    
    @@keyframes slideInUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
    
    .menu-row {
        animation: slideInUp 0.3s ease-out;
    }
</style>
